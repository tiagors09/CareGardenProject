import 'package:flutter/material.dart';

class ListaPlantas extends StatelessWidget {
  const ListaPlantas({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Lista Plantas'),
    );
  }
}
